# Mon premier RAG - Mini-TP M2 MD5

RAG minimal mais complet : ChromaDB + sentence-transformers + Groq + agent moderateur.

## Installation

python -m venv venv
source venv/Scripts/activate
pip install -r requirements.txt
cp .env.example .env

## Lancer les tests

python main.py